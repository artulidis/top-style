user  root;
worker_processes  auto;

events {
    worker_connections  1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;

    server {
        listen 80;
        listen [::]:80;
        server_name topstylebykatenoda.com www.topstylebykatenoda.com;

        location /.well-known/acme-challenge/ {
            root /var/www/html;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        listen [::]:443 ssl;
        server_name topstylebykatenoda.com www.topstylebykatenoda.com;

        ssl_certificate /etc/letsencrypt/live/topstylebykatenoda.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/topstylebykatenoda.com/privkey.pem;

        location / {
            proxy_pass http://flask:5000;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}